# Discord Configuration
DISCORD_TOKEN=your_discord_bot_token
# Required for registering slash commands (guild-specific). Get from Server Settings -> right-click server -> Copy ID (Developer Mode on)
GUILD_ID=your_discord_server_id
CHANNEL_ID=channel_for_stream_notifications
ROLE_ID=role_to_ping_for_live_alerts

# Twitch API Configuration (Client App Credentials)
CLIENT_ID=your_twitch_client_id
CLIENT_SECRET=your_twitch_client_secret

# Optional: Default Twitch username (can add more via slash commands; max 50)
TWITCH_USERNAME=default_streamer

# Optional: Polling interval in seconds (default: 60)
CHECK_INTERVAL=60

# Optional: Bot profile links (shown in bot's activity status)
TWITCH_LINK=https://twitch.tv/yourusername
YOUTUBE_LINK=https://youtube.com/@yourusername
DISCORD_LINK=https://discord.gg/yourserver

# Audit log: set the audit channel in Discord with /setauditchannel (or leave empty to disable)
